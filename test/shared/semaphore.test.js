"use strict";
const { Semaphore } = require("../../src/shared/semaphore");

describe("semaphore", () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test("make exclusive", async () => {
    // TODO code on
  });
});
